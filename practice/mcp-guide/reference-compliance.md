# 부록: 명세 준수표 — 세 practice는 명세를 어디까지 지키나

## 읽는 법

행은 명세 항목이고, 열은 세 practice([official](../mcp-security-authn-official/README.md)·[chat-memory](../mcp-security-authn-chat-memory/README.md)·[community](../mcp-security-authn-community/README.md))다.
official 칸은 agent(`shop-agent`)와 public client `local-client`를 함께 판정하고, 나머지 두 practice에는 `local-client`가 없다.
"요구 수준" 칸은 원문 단어(MUST 등)를 그대로 쓰고, 명세가 요구하지 않는 항목은 "참고", 요구 수준 단어가 없는 문장은 "표시 없음"이라고 쓴다.
판정은 예(지킨다), **아니오**(어긴다), 다루지 않음(그 기능을 쓰지 않는다), 해당 없음(조건이 생기지 않는다) 넷이고, 뒤에 근거인 클래스·설정·테스트(`클래스#메서드`)·캡처 번호를 붙인다.
"같음"은 왼쪽 칸과 같다는 뜻이고, "코드로 판정"은 캡처 없이 코드를 읽고 내린 판정이라는 뜻이다.
캡처 번호 `C<n>`·`S<n>`·`P<n>`은 `docs/superpowers/captures/`의 [walkthrough](../../docs/superpowers/captures/2026-09-12-official.txt)·[supplement](../../docs/superpowers/captures/2026-09-16-official-supplement.txt)·[public client](../../docs/superpowers/captures/2026-09-25-official-public-client.txt) 캡처의 단계 번호이고, chat-memory·community 캡처도 같은 번호를 쓴다.
`local-client`의 `[1]`~`[5]`는 [local-client 캡처](../../docs/superpowers/captures/2026-09-26-local-client.txt)의 줄이다.
"설명" 열은 그 내용을 설명하는 장이고, 공격은 [8장](08-security.md), 기준 버전(transport는 2025-11-25, authorization은 2025-11-25에 2026-07-28 추가분)은 [9장](09-versions.md)에 있다.

## 구현 위치 지도

세 practice는 Authorization Server, MCP Server, agent를 서로 다른 프로세스로 띄운다.
MCP Server는 OAuth 2.1 resource server이고, agent와 `local-client`는 OAuth 2.1 client다([MCP 2025-11-25 Authorization — Roles](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#roles)).
agent는 `client_secret`을 가진 confidential client이고, `local-client`는 비밀이 없는 public client다([OAuth 2.1 §2.1](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-2.1)).
MCP Server가 token을 검증만 하고 Authorization Server를 따로 두는 구성은 2025-06-18부터의 방식이다([MCP 2025-03-26 Authorization](https://modelcontextprotocol.io/specification/2025-03-26/basic/authorization), [MCP 2025-06-18 Changelog](https://modelcontextprotocol.io/specification/2025-06-18/changelog), [9장](09-versions.md)).

| | official | chat-memory | community |
|---|---|---|---|
| Authorization Server (issuer) | `http://localhost:9010` | `http://localhost:9020` | `http://localhost:9000` |
| MCP Server (resource) | `http://localhost:8111/mcp` | `http://localhost:8131/mcp` | `http://localhost:8101/mcp` |
| agent | `http://localhost:8110` | `http://localhost:8130` | `http://localhost:8100` |
| agent의 client_id | `official-shop-agent` | `memory-agent` | `shop-agent` |
| agent의 redirect URI | `http://localhost:8110/login/oauth2/code/authserver` | `http://localhost:8130/login/oauth2/code/authserver` | `http://localhost:8100/login/oauth2/code/authserver` |
| public client | `local-mcp-client`, redirect URI `http://127.0.0.1:8123/callback` | 같음 | 같음 |
| public client 프로그램 | `local-client` | 없음 — 캡처 스크립트의 curl만 | 없음 — 같음 |
| login | `user` / `password` | `alice` / `alice`, `bob` / `bob` | `user` / `password` |
| 구성 방식 | filter chain과 bean을 직접 정의 | official과 같은 클래스 + 사용자별 대화 기억 | spring-ai-community `mcp-security` module 0.1.14 자동 설정 + 확장점 |

chat-memory의 클래스는 official과 package(`dev.starryeye.memoryauthn.*`)만 다르고, 다른 곳은 아래 표에 따로 적었다.
`local-client`의 클래스는 `practice/mcp-security-authn-official/local-client/src/main/java/dev/starryeye/localclient/`에 있다.

| module | 하는 일 | official · chat-memory | community | 장 |
|---|---|---|---|---|
| `auth-server` | 설정 진입점 | `AuthorizationServerConfig` — filter chain을 직접 정의 | `McpAuthorizationStandardConfig`(`Customizer<McpAuthorizationServerConfigurer>`), OIDC endpoint를 켜는 `OidcDiscoveryConfig` | [3장](03-discovery.md) |
| | client 등록, PKCE 강제 | `application.yml` — agent client와 `local-mcp-client`, `require-proof-key: true` | 같음 | [4장](04-client-registration.md) |
| | metadata 광고(`iss` 지원, `none`, client 인증 signature 알고리즘) | `AuthorizationServerConfig`의 metadata customizer | `McpAuthorizationStandardConfig` | [3장](03-discovery.md), [4장](04-client-registration.md) |
| | `resource` 허용 목록, authorization request 검증 | `McpResourceProperties`, `ResourceIndicatorValidator` | 같음 | [5장](05-authorization-and-token.md) |
| | access token `aud`, token request의 `resource` 검증 | `ResourceAudienceTokenCustomizer` | 같음 + ID token `aud`를 client_id로 되돌리기, `SingleResourceTokenRequestConverter` | [5장](05-authorization-and-token.md) |
| | authorization response의 `iss` | `IssuerIdentifyingAuthorizationResponseHandler` | 같음 | [5장](05-authorization-and-token.md) |
| | `invalid_client`의 `WWW-Authenticate` | `ClientAuthenticationChallengeFailureHandler` | 같음 | [5장](05-authorization-and-token.md) |
| | public client가 consent를 건너뛰지 못하게 하기 | `PublicClientScopeValidator`, `PublicClientConsentService` | 같음 | [5장](05-authorization-and-token.md), [8장](08-security.md) |
| | DCR | 켜지 않음(Spring 기본값) | 끔 — `dynamic-client-registration.enabled: false` | [4장](04-client-registration.md) |
| `shop-mcp-server` | PRM, `401` challenge | `SecurityConfig`의 `protectedResourceMetadata(...)`, `resourceMetadataEntryPoint` | module `McpServerOAuth2Configurer#protectedResourceMetadataCustomizer` + 같은 Spring 진입점 | [3장](03-discovery.md) |
| | token 검증(signature·`iss`·`aud`·`exp`) | `application.yml`의 `issuer-uri`·`audiences` | Boot `JwtDecoder` + module `AudienceValidationJwtDecoder` | [6장](06-mcp-call-and-validation.md) |
| | `Origin`·`Host` 검사(인증 앞) | `McpTransportSecurityFilter` + `McpTransportConfig` | module `OriginValidationFilter` | [6장](06-mcp-call-and-validation.md) |
| | `MCP-Protocol-Version` 검사(인증 뒤) | `McpProtocolVersionFilter` + `McpTransportConfig` | `McpProtocolVersionFilter` + `McpProtocolVersionFilterConfig` | [6장](06-mcp-call-and-validation.md) |
| | session을 사용자에 묶기 | official은 묶지 않음, chat-memory는 `McpSessionBindingFilter` | 묶지 않음 — module `sessionBinding()`을 켜지 않는다 | [6장](06-mcp-call-and-validation.md) |
| | tool | `ProductTools`, `ProductRepository` | 같음 | [1장](01-mcp-basics.md) |
| `shop-agent` | discovery | `McpAuthorizationDiscovery`, `DiscoveredAuthorization` | 같음 + challenge와 PRM은 module `McpMetadataDiscoveryService` | [3장](03-discovery.md) |
| | credentials와 issuer binding | `DiscoveredClientRegistrationRepository`, `McpAuthorizationProperties` | 같음 | [4장](04-client-registration.md) |
| | authorization request의 PKCE·`resource` | `SecurityConfig#authorizationRequestResolver`, `ResourceIndicators` | 같음 | [5장](05-authorization-and-token.md) |
| | token·refresh request의 `resource` | `McpSecurityConfig` — `RestClientAuthorizationCodeTokenResponseClient`, `RestClientRefreshTokenTokenResponseClient` | 같음 | [5장](05-authorization-and-token.md) |
| | callback의 `iss` 확인 | `AuthorizationResponseIssuerFilter`, `LoginFailureHandler` | 같음 | [5장](05-authorization-and-token.md) |
| | MCP 요청에 token 붙이기 | official은 `OAuth2TokenAttachingRequestCustomizer`, `SecurityMcpTransportContextProvider`. chat-memory는 사용자별 client를 여는 `UserMcpClients` + 주인에 묶인 customizer | module `OAuth2AuthorizationCodeSyncHttpRequestCustomizer`, `ChatController`의 `AuthenticationMcpTransportContextProvider.writeToReactorContext()` | [6장](06-mcp-call-and-validation.md) |
| | 만료 token refresh | `McpSecurityConfig#authorizedClientManager`(`AuthorizedClientServiceOAuth2AuthorizedClientManager`) | `McpSecurityConfig#authorizedClientManager`(`DefaultOAuth2AuthorizedClientManager`) | [5장](05-authorization-and-token.md) |
| | MCP transport | SDK `HttpClientStreamableHttpTransport` | 같음 | [1장](01-mcp-basics.md) |
| `local-client` (official에만) | discovery | `Discovery` | 없음 | [3장](03-discovery.md), [7장](07-local-client.md) |
| | callback server | `LoopbackCallbackServer` | 없음 | [7장](07-local-client.md) |
| | PKCE, authorization request, browser 열기 | `Pkce`, `AuthorizationRequest`, `Browser` | 없음 | [5장](05-authorization-and-token.md), [7장](07-local-client.md) |
| | callback 확인 | `AuthorizationResponse` | 없음 | [7장](07-local-client.md) |
| | token request, MCP 호출 | `TokenClient`, `McpCalls` | 없음 | [7장](07-local-client.md) |

## 준수표

| # | 항목 | 요구 수준 | official | chat-memory | community | 설명 |
|---|---|---|---|---|---|---|
| 1 | PRM 제공, `resource`는 canonical URI이고 요청 URL과 일치 | MUST ([MCP — Authorization Server Location](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#authorization-server-location) · [RFC 9728 §3.3](https://www.rfc-editor.org/rfc/rfc9728#section-3.3) · [§7.3](https://www.rfc-editor.org/rfc/rfc9728#section-7.3)) · canonical URI는 [MCP — Canonical Server URI](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#canonical-server-uri) | 예 — `SecurityConfig`의 `protectedResourceMetadata(...)`, C2 | 예 — 같음 | 예 — module `McpServerOAuth2Configurer#protectedResourceMetadataCustomizer`, C2 | [3장](03-discovery.md) |
| 2 | `401`의 `resource_metadata` | MUST ([MCP — PRM Discovery Requirements](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#protected-resource-metadata-discovery-requirements)) | 예 — `SecurityConfig#resourceMetadataEntryPoint`, C1 | 예 — 같음, C1 `http://localhost:8131/.well-known/oauth-protected-resource/mcp` | 예 — module 기본 진입점 대신 같은 Spring 진입점, C1 `http://localhost:8101/.well-known/oauth-protected-resource/mcp` | [3장](03-discovery.md) |
| 3 | client의 PRM discovery와 fallback 순서 | MUST ([MCP — PRM Discovery Requirements](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#protected-resource-metadata-discovery-requirements)) | 예 — agent `McpAuthorizationDiscovery#protectedResourceMetadata`. `local-client` `Discovery`도 header → 경로형 → 루트형 순서다 | 예 — 같음 | 예 — module `McpMetadataDiscoveryService#getMcpMetadata` | [3장](03-discovery.md) |
| 4 | Authorization Server Metadata discovery 순서와 `issuer` 검증 | MUST ([MCP — Authorization Server Metadata Discovery](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#authorization-server-metadata-discovery) · [2026-07-28](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/authorization-server-discovery#authorization-server-metadata-discovery)) | 예 — agent `McpAuthorizationDiscovery#metadataUrls`, `local-client` `Discovery#metadataUrls` | 예 — 같음 | 예 — `McpAuthorizationDiscovery`가 이어받는다 | [3장](03-discovery.md) |
| 5 | `code_challenge_methods_supported` 확인 | MUST ([MCP — Authorization Code Protection](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#authorization-code-protection)) | 예 — `McpAuthorizationDiscoveryTest#PKCE_S256_을_광고하지_않으면_진행하지_않는다`, `DiscoveryTest#S256을_지원하지_않으면_멈춘다` | 예 — 같음 | 예 — 같음 | [3장](03-discovery.md) |
| 6 | PKCE `S256` | MUST ([OAuth 2.1 §7.5.2](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-7.5.2) · [MCP 2026-07-28 — Authorization Code Protection](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/security-considerations#authorization-code-protection)) | 예 — agent `withPkce()`, `local-client` `Pkce`, Authorization Server `require-proof-key: true`, C17 | 예 — 같음 | 예 — 같음 | [5장](05-authorization-and-token.md) |
| 7 | `resource` — authorization·token·refresh request | MUST ([MCP — Resource Parameter Implementation](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#resource-parameter-implementation)) | 예 — agent `ResourceIndicators`, C5·C6·C11. `local-client`는 `AuthorizationRequest`와 `TokenClient`가 넣는다(refresh 없음) | 예 — 같음 | 예 — 같음 | [5장](05-authorization-and-token.md), [7장](07-local-client.md) |
| 8 | token audience 발급과 검증 | MUST ([MCP — Token Audience Binding and Validation](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#token-audience-binding-and-validation) · [MCP 2026-07-28 — Access Token Privilege Restriction](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/security-considerations#access-token-privilege-restriction)) | 예 — `ResourceAudienceTokenCustomizer` + `audiences`, C12 | 예 — 같음 | 예 — module `AudienceValidationJwtDecoder`가 기대 `aud`를 요청 URL로 계산한다(`127.0.0.1:8101`로 부르면 `http://127.0.0.1:8101/mcp`). 그래서 Host 검증(`OriginValidationFilter`)이 먼저 와야 한다(`McpAuthorizationStandardTest#Host_를_바꾸고_그_Host_용_token_을_실어도_audience_계산_전에_421이다`) | [5장](05-authorization-and-token.md), [6장](06-mcp-call-and-validation.md) |
| 9 | RFC 9207 `iss` — 보내기·광고·요청별 issuer 기록·client 검증 | SHOULD (보내기, MCP 2026-07-28) · MUST (보내면 광고, [RFC 9207 §2.3](https://www.rfc-editor.org/rfc/rfc9207#section-2.3)) · MUST (요청별 기록, [Authorization Response Validation](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization#authorization-response-validation)) · MUST (있으면 검증, [RFC 9207 §2.4](https://www.rfc-editor.org/rfc/rfc9207#section-2.4)) | 예(agent의 기록 방식은 문구와 다름) — `IssuerIdentifyingAuthorizationResponseHandler`, agent `AuthorizationResponseIssuerFilter`(S18·S19), `local-client` `AuthorizationResponse#code`. agent의 요청별 기록은 issuer 대신 `registrationId`를 담고, `local-client`는 issuer를 `state`·`code_verifier`와 함께 들고 있다 | 예(agent의 기록 방식은 문구와 다름) — 같음 | 예(agent의 기록 방식은 문구와 다름) — 같음(`McpAuthorizationStandardConfig`) | [5장](05-authorization-and-token.md), [8장](08-security.md) |
| 10 | credentials의 issuer binding | MUST · MUST NOT · SHOULD ([MCP 2026-07-28 — Authorization Server Binding](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/client-registration#authorization-server-binding) · [Authorization Server Location](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/authorization-server-discovery#authorization-server-location)) · 고르는 기준은 [RFC 9728 §7.6](https://www.rfc-editor.org/rfc/rfc9728#section-7.6) | 예 — agent `DiscoveredClientRegistrationRepository`, `McpAuthorizationDiscovery#discover(String, String)`는 `authorization_servers`의 첫 값을 설정의 issuer와 비교한다. `local-client` `Discovery#discover`는 목록 전체에서 `--issuer`를 찾는다(`DiscoveryTest#PRM이_모르는_Authorization_Server를_가리키면_metadata를_요청하지_않는다`) | 예 — 같음 | 예 — 같음 | [4장](04-client-registration.md), [7장](07-local-client.md) |
| 11 | `Origin` 검증, `Host` 검증 | MUST (`Origin`, [Transports — Security Warning](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports#security-warning)) · 규정 없음 (`Host`, [RFC 9110 §15.5.20](https://www.rfc-editor.org/rfc/rfc9110#section-15.5.20)) | 예 — `McpTransportSecurityFilter`가 Spring Security 앞에서 SDK 검증기를 부르고 허용 `Origin`은 없다, C13·S14, `McpAuthorizationStandardTest#token_이_없어도_허용되지_않은_Origin_은_인증보다_먼저_403이다`. Spring AI 자동 구성의 transport 검증기는 `ServerTransportSecurityValidator.NOOP`이다 | 예 — 같음 | 예 — module `OriginValidationFilter`(인증 앞), 허용 `Origin`은 `http://localhost:8101`, 같은 테스트로 `403`·`421` | [6장](06-mcp-call-and-validation.md), [8장](08-security.md) |
| 12 | HTTPS | MUST ([MCP — Communication Security](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#communication-security) · [2026-07-28](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/security-considerations#communication-security) · [OAuth 2.1 §1.5](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-1.5)) | **아니오** — 모두 `http://localhost` | **아니오** — 같음 | **아니오** — 같음 | [8장](08-security.md) |
| 13 | token passthrough 금지 | MUST · MUST NOT ([MCP — Token Handling](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#token-handling) · [Token Passthrough](https://modelcontextprotocol.io/specification/2025-11-25/basic/security_best_practices#token-passthrough)) | 예 — downstream API 없음. agent `OAuth2TokenAttachingRequestCustomizer`와 `local-client` `McpCalls`는 그 MCP Server의 Authorization Server가 발급한 token만 붙인다 | 예 — 같음 | 예 — module `OAuth2AuthorizationCodeSyncHttpRequestCustomizer` | [8장](08-security.md) |
| 14 | Dynamic Client Registration(RFC 7591) | MAY ([MCP — Dynamic Client Registration](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#dynamic-client-registration)) · deprecated ([MCP 2026-07-28 Client Registration](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/client-registration)) | 다루지 않음 — 켜지 않음, C3에 `registration_endpoint` 없음 | 다루지 않음 — 같음 | 다루지 않음 — `dynamic-client-registration.enabled: false` | [4장](04-client-registration.md), [9장](09-versions.md) |
| 15 | Client ID Metadata Document(CIMD) | SHOULD ([MCP — Client ID Metadata Documents](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#client-id-metadata-documents)) | 다루지 않음 — HTTPS `client_id`가 전제 | 다루지 않음 — 같음 | 다루지 않음 — 같음 | [4장](04-client-registration.md) |
| 16 | scope 설계·step-up authorization | SHOULD ([Scope Challenge Handling](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#scope-challenge-handling)) | 다루지 않음 — 인증된 요청은 모든 tool 허용, `401`과 PRM에 scope가 없다. client의 scope 선택은 37번 | 다루지 않음 — 같음 | 다루지 않음 — 같음 | [8장](08-security.md) |
| 17 | RFC 9068 access token 프로파일 | 참고 — MCP는 요구하지 않음 ([RFC 9068 §2.1](https://www.rfc-editor.org/rfc/rfc9068#section-2.1) · [§2.2](https://www.rfc-editor.org/rfc/rfc9068#section-2.2) · [RFC 8693 §4.2](https://www.rfc-editor.org/rfc/rfc8693#section-4.2)) | 아니오(불일치) — header에 `typ` 없음(C6), `client_id` claim 없음, `scope`가 JSON 배열(C6-1) | 아니오(불일치) — 같음 | 아니오(불일치) — 같음 | [5장](05-authorization-and-token.md) |
| 18 | `invalid_client` `401`의 `WWW-Authenticate` | MUST ([RFC 6749 §5.2](https://www.rfc-editor.org/rfc/rfc6749#section-5.2) · [OAuth 2.1 §3.2.4](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-3.2.4)) | 예 — `ClientAuthenticationChallengeFailureHandler`, S8. Spring 기본 동작은 이 header를 붙이지 않는다([spring-security #18285](https://github.com/spring-projects/spring-security/issues/18285)) | 예 — 같음 | 예 — `McpAuthorizationStandardConfig`가 같은 handler를 쓴다 | [5장](05-authorization-and-token.md) |
| 19 | SSE event `id`의 session 내 유일성 | MUST ([Transports — Resumability and Redelivery](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports#resumability-and-redelivery)) | **아니오** — event `id`가 session ID, C9·C10 | **아니오** — 같음(같은 SDK) | **아니오** — 같음(같은 SDK) | [1장](01-mcp-basics.md), [8장](08-security.md) |
| 20 | `token_endpoint`·`revocation_endpoint`·`introspection_endpoint`의 `_auth_signing_alg_values_supported` | 조건부 MUST ([RFC 8414 §2](https://www.rfc-editor.org/rfc/rfc8414#section-2)) | 예 — `AuthorizationServerConfig`의 metadata customizer, C3 | 예 — 같음 | 예 — `McpAuthorizationStandardConfig` | [부록 API](reference-api.md#get-well-knownoauth-authorization-server--authorization-server-metadata) |
| 21 | discovery 결과 재검증 | SHOULD ([RFC 9728 §5.2](https://www.rfc-editor.org/rfc/rfc9728#section-5.2)) | **아니오** — agent는 프로세스 수명 동안 캐시하고, 실행 중 받은 `401`로 PRM을 다시 읽지 않는다. `local-client`는 실행마다 discovery를 한다 | **아니오** — 같음 | **아니오** — 같음 | [3장](03-discovery.md) |
| 22 | `GET /mcp` stream의 응답 시작 확인 | 참고 — 규정 없음 | 해당 없음 — 첫 event 전까지 header가 나가지 않는다(S11) | 해당 없음 — 같음 | 해당 없음 — 같음 | [부록 API](reference-api.md#get-mcp--서버가-보내는-메시지의-sse-stream) |
| 23 | public client의 `client_id`, PKCE 강제, `none` 광고 | REQUIRED ([OAuth 2.1 §4.1.3](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-4.1.3)) · MUST · OPTIONAL ([RFC 8414 §2](https://www.rfc-editor.org/rfc/rfc8414#section-2)) | 예 — `local-mcp-client` 등록과 `none` 광고, P5. `local-client` `TokenClient`는 `Authorization` header 없이 `client_id`와 `code_verifier`를 보낸다(`[4]`) | 예 — 같음 | 예 — `McpAuthorizationStandardConfig`, P5 | [4장](04-client-registration.md), [7장](07-local-client.md) |
| 24 | loopback redirect의 포트 허용 | MUST ([RFC 8252 §7.3](https://www.rfc-editor.org/rfc/rfc8252#section-7.3) · [OAuth 2.1 §8.4.2](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-8.4.2)) | 예 — Spring `OAuth2AuthorizationCodeRequestAuthenticationValidator`, P10. `local-client`는 운영체제가 고른 포트로 받는다(`[3]`의 `http://127.0.0.1:59597/callback`) | 예 — 같음 | 예 — 같음 | [4장](04-client-registration.md), [7장](07-local-client.md) |
| 25 | 신원을 확인할 수 없는 client의 re-consent | SHOULD · SHOULD NOT ([OAuth 2.1 §7.3.1](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-7.3.1)) | 예 — `PublicClientScopeValidator`(scope 없음·`openid` 하나 → `invalid_scope`, P8-1) + `PublicClientConsentService`(저장된 consent 무시, P8) | 예 — 같음 | 예 — 같음 | [5장](05-authorization-and-token.md), [8장](08-security.md) |
| 26 | public client refresh token의 회전 또는 sender-constrained | MUST ([OAuth 2.1 §4.3.1](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-4.3.1)) | 해당 없음 — refresh token을 발급하지 않는다, P7 | 해당 없음 — 같음 | 해당 없음 — 같음 | [4장](04-client-registration.md), [5장](05-authorization-and-token.md) |
| 27 | 서버에 배포된 MCP client의 SSRF 위험 고려와 대응 | MUST, 대응 네 가지는 SHOULD ([Security Best Practices — SSRF](https://modelcontextprotocol.io/specification/2025-11-25/basic/security_best_practices#server-side-request-forgery-ssrf)) | **아니오** — agent는 issuer binding 확인을 Authorization Server Metadata 요청 앞에 둘 뿐이고(`McpAuthorizationDiscoveryTest#PRM_의_Authorization_Server_가_자격증명의_issuer_가_아니면_metadata_를_요청하지_않는다`), `local-client`는 서버에 배포된 client가 아니라 대상이 아니다. challenge·PRM URL은 scheme·사설 IP 확인 없이 요청하고, redirect를 따라가지 않는 것은 classpath에서 고른 Reactor Netty client의 기본값이라 httpclient5가 들어오면 바뀐다 | **아니오** — 같음 | **아니오** — issuer binding 순서는 같다. PRM URL만 `DefaultUrlValidator(true)`로 HTTPS·loopback HTTP인지 보고, 사설 IP는 막지 않는다 | [8장](08-security.md) |
| 28 | MCP session을 사용자에 묶기 | SHOULD ([Security Best Practices — Session Hijacking](https://modelcontextprotocol.io/specification/2025-11-25/basic/security_best_practices#session-hijacking)) | **아니오** — agent가 MCP client 하나를 모든 사용자와 같이 쓴다 | 예 — `McpSessionBindingFilter` + `UserMcpClients` | **아니오** — official과 같은 이유, module `sessionBinding()`을 켜지 않는다 | [6장](06-mcp-call-and-validation.md), [8장](08-security.md) |
| 29 | session ID가 인증을 대신하지 않음, 예측할 수 없는 값 | MUST · MUST NOT ([Security Best Practices — Session Hijacking](https://modelcontextprotocol.io/specification/2025-11-25/basic/security_best_practices#session-hijacking)) | 예 — 모든 요청에서 token 검증, SDK가 `UUID.randomUUID()`로 session ID 생성 | 예 — 같음 | 예 — 같음 | [6장](06-mcp-call-and-validation.md), [8장](08-security.md) |
| 30 | 로컬 서버는 127.0.0.1에 bind | SHOULD ([Transports — Security Warning](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports#security-warning)) | 예 — `server.address: 127.0.0.1`, S21([listen 캡처](../../docs/superpowers/captures/2026-09-25-listen-addresses.txt)) | 예 — 같음 | 예 — 같음 | [6장](06-mcp-call-and-validation.md) |
| 31 | 지원하지 않는 `MCP-Protocol-Version`에 `400` | MUST ([Transports — Protocol Version Header](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports#protocol-version-header)) | 예 — `McpProtocolVersionFilter`, C15. `2026-07-28`에도 `400`과 `-32600`으로 답한다 | 예 — 같음 | 예 — `McpProtocolVersionFilterConfig` | [6장](06-mcp-call-and-validation.md), [9장](09-versions.md) |
| 32 | redirect URI 정확 일치, 틀린 주소로 redirect 금지 | MUST · MUST NOT ([Open Redirection](https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/security-considerations#open-redirection) · [OAuth 2.1 §4.1.1](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-4.1.1) · [§4.1.2.1](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-4.1.2.1)) | 예 — Spring `OAuth2AuthorizationCodeRequestAuthenticationValidator`, S4·P10-1. login하지 않은 browser는 `/error`도 login을 요구해 `302` → `/login`이고, 틀린 주소로는 가지 않는다 | 예 — 같음 | 예 — 같은 Spring 검증기 | [4장](04-client-registration.md), [8장](08-security.md) |
| 33 | authorization URL scheme 검증(`http`는 loopback만) | MUST ([Security Best Practices — OAuth Authorization URL Validation](https://modelcontextprotocol.io/specification/2025-11-25/basic/security_best_practices#oauth-authorization-url-validation)) | 예 — agent `McpAuthorizationDiscovery#requireHttpUrl`, `local-client` `Discovery#requireEndpoint`. scheme은 `http`·`https`만, `http`는 loopback host만 받는다 | 예 — 같음 | 예 — 같음 | [3장](03-discovery.md), [8장](08-security.md) |
| 34 | MCP Server의 JWT 검증 — signature·`iss`·`exp`, RFC 9068의 `typ` | MUST ([MCP — Token Handling](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#token-handling) · [OAuth 2.1 §5.2](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-5.2)) · `typ`은 참고 ([RFC 9068 §4](https://www.rfc-editor.org/rfc/rfc9068#section-4) MUST, 그 프로파일의 token일 때) | 예(`typ` 제외) — `issuer-uri`의 key로 signature(`alg: none` 거절)와 `iss`를, `JwtTimestampValidator`로 `exp`를 본다(S3, `McpAuthorizationStandardTest#만료된_토큰은_거부한다`). `typ`은 Spring 기본 `JwtTypeValidator.jwt()`라 없거나 `JWT`일 때만 통과하고 `at+jwt`는 거절하므로, Authorization Server만 RFC 9068 프로파일(17번)로 바꾸면 검증이 깨진다 | 예(`typ` 제외) — 같음 | 예(`typ` 제외) — Boot가 만든 같은 decoder를 module에 넘긴다 | [6장](06-mcp-call-and-validation.md) |
| 35 | native app의 loopback callback server — listen 주소, 포트 수명, IP 버전 | 표시 없음 (소문자 should, [RFC 8252 §8.3](https://www.rfc-editor.org/rfc/rfc8252#section-8.3)) · SHOULD NOT · RECOMMENDED ([OAuth 2.1 §8.4.2](https://datatracker.ietf.org/doc/html/draft-ietf-oauth-v2-1-13#section-8.4.2)) | **아니오**(RECOMMENDED) — `LoopbackCallbackServer`는 `127.0.0.1`에만 listen하고 IPv6 `[::1]`은 시도하지 않으며, 시도하려면 Authorization Server에도 `http://[::1]:8123/callback`을 등록해야 한다. 포트는 callback을 받은 뒤에도 MCP 호출이 끝날 때까지 열려 있다(`Main`의 try-with-resources) | 해당 없음 — `local-client`가 없다 | 해당 없음 — 같음 | [7장](07-local-client.md) |
| 36 | 모든 MCP 요청의 `Authorization` header — session 종료 `DELETE` 포함 | MUST ([MCP — Token Requirements](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#token-requirements)) | **아니오**(agent, 코드로 판정) — 앱 종료 때 공유 client가 보내는 `DELETE`는 transport context가 비어 `OAuth2TokenAttachingRequestCustomizer#customize`가 header를 붙이지 않는다(`CloseableMcpSyncClients#close` → `McpSyncClient#close` → `HttpClientStreamableHttpTransport#createDelete`). `local-client`는 예 — `McpCalls`가 transport 기본 요청에 넣은 header가 `DELETE`에도 복사된다 | 예(코드로 판정) — client마다 주인을 가진 `OAuth2TokenAttachingRequestCustomizer`가 context와 상관없이 token을 붙이고, `UserMcpClients`가 logout·HTTP session 종료·앱 종료 때 `closeGracefully()`로 닫는다. 테스트 `OAuth2TokenAttachingRequestCustomizerTest#transport_context_없이_나가는_session_종료_DELETE_에도_토큰을_붙인다` | **아니오**(코드로 판정) — module `OAuth2AuthorizationCodeSyncHttpRequestCustomizer#customize`도 context에 `Authentication`과 servlet 요청이 없으면 header 없이 돌아간다. 종료 경로는 official과 같다 | [6장](06-mcp-call-and-validation.md), [7장](07-local-client.md) |
| 37 | client의 scope 선택 — `401`의 `scope` → PRM의 `scopes_supported` → `scope` 생략 | SHOULD ([Scope Selection Strategy](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization#scope-selection-strategy)) | **아니오** — `401`과 PRM에 scope가 없어 `scope`를 빼야 하지만, agent는 OIDC login 설정의 `openid profile`을, `local-client`는 `Main.SCOPE`의 `openid profile`을 보낸다. OIDC login에는 `openid`가, public client에는 25번의 `PublicClientScopeValidator`가 요구하는 `openid` 밖의 scope가 필요하다 | **아니오** — agent는 같음 | **아니오** — agent는 같음 | [7장](07-local-client.md) |
| 38 | session 오류 응답 본문의 정보 노출 | 참고 — 전송 명세는 본문 형식을 정하지 않음, `id` 없는 JSON-RPC 오류는 MAY ([JSON-RPC 2.0 §5.1](https://www.jsonrpc.org/specification#error_object)) | 아니오(약점) — session ID 없는 `400`(C14, `-32601`)과 모르는 session의 `404`(S13, `-32603`) 본문에 Java `stackTrace`가 담긴다. `-32601`은 JSON-RPC의 Method not found라 SDK의 선택이다 | 아니오(약점) — 같음(같은 SDK) | 아니오(약점) — 같음(같은 SDK) | [부록 API](reference-api.md#post-mcp--bearer-token) |

## 남은 위반

MUST 위반은 네 행이다.
12·19번은 세 practice에 똑같이 남아 있다.
27번도 세 practice 모두에 있고, community는 PRM URL의 scheme을 더 본다.
36번은 official과 community의 agent에 있고, chat-memory의 agent와 `local-client`에는 없다.

| # | 항목 | 지금 | 이유 |
|---|---|---|---|
| 12 | HTTPS | Authorization Server, MCP Server, agent가 모두 `http://localhost`로 돈다 | 요청과 응답을 그대로 보려는 학습용 선택이다. 요청은 loopback 주소 안에서만 오간다 |
| 19 | SSE event `id`의 유일성 | 한 session의 event `id`가 모두 session ID다 | 값을 정하는 Spring AI `WebMvcStreamableServerTransportProvider`가 `final`이고, 안의 session transport는 `private`이다. 고치려면 transport를 fork해야 한다 |
| 27 | 서버에서 도는 agent의 SSRF 대응 | issuer binding 순서와 authorization URL scheme 확인만 있다. community는 PRM URL의 scheme도 본다 | 모든 구성 요소가 한 기기의 `localhost`에서 도는 학습 환경이다 |
| 36 | agent가 앱 종료 때 보내는 session `DELETE`의 token | 코드상 token 없이 보내므로, MCP Server에 닿으면 `401`이다. 기다리지 않고 보내는 요청이라 나가지 못할 수도 있고, 어느 쪽이든 session은 MCP Server가 내려갈 때까지 남는다 | token customizer가 요청을 일으킨 사용자를 transport context에서 찾는데, 종료 때의 `DELETE`에는 context가 없다. chat-memory처럼 사용자마다 client를 두고 그 주인의 token을 붙이면 풀린다 |

- SHOULD 수준에서 지키지 않는 것은 21번(discovery 결과 재검증), 28번(official·community의 session 묶기), 35번(`local-client`의 IPv6 시도), 37번(client의 scope 선택)이다. 16번은 scope를 설계하지 않아 다루지 않는다.
- 17·22·38번과 34번의 `typ`은 MCP가 요구하지 않거나 규정이 없는 참고 항목이라 위반으로 세지 않는다.
- 18·20·25번은 Spring 기본 동작이 명세에 못 미치는 곳을 practice가 직접 채운 것이다.
- official이 지키지 못한 것을 공격 쪽에서 본 설명은 [8장](08-security.md#810-official이-지키지-못한-것)에 있다.

[← 부록: API 사전](reference-api.md) · [목차](README.md)
